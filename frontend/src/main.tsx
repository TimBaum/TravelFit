import ReactDOM from 'react-dom/client'
import './index.css'
import App from './App.tsx'

/*entry point to our Web App, liked to the root component App.tsx*/
ReactDOM.createRoot(document.getElementById('root')!).render(<App />)
